import React, {memo} from 'react';
import classes from './styles.module.scss';

const Footer = () => {
  return (
    <footer>
      <h2>I am sexy and I Know it</h2>
    </footer>
  );
};

export default memo(Footer);